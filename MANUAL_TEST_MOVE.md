#!/bin/bash
# Manual test guide for session move functionality

echo "=== Manual Test Guide for Session Move ==="
echo ""
echo "This test verifies:"
echo "  1. Prefix key is centralized and easy to change"
echo "  2. Prefix + Up/Down moves sessions in sidebar"
echo "  3. Order persists daemon-side"
echo "  4. Selection remains stable when moving"
echo ""
echo "Setup:"
echo "  1. Stop any running mbulet daemons:"
echo "     pkill -f 'mbulet.*daemon'"
echo ""
echo "  2. Start fresh daemon:"
echo "     ./target/release/mbulet daemon /tmp/mbulet-test.sock &"
echo "     sleep 1"
echo ""
echo "  3. Start client:"
echo "     ./target/release/mbulet /tmp/mbulet-test.sock"
echo ""
echo "Test Steps:"
echo ""
echo "  A. Create multiple sessions:"
echo "     - In sidebar, press 'n' to create new sessions"
echo "     - Create 3-4 sessions named session-1, session-2, etc."
echo ""
echo "  B. Test moving sessions:"
echo "     - Select a session in the middle (use j/k to navigate)"
echo "     - Press Ctrl+B, then Up arrow → session moves up"
echo "     - Press Ctrl+B, then Down arrow → session moves down"
echo "     - Try moving first session up (should not move)"
echo "     - Try moving last session down (should not move)"
echo ""
echo "  C. Verify selection stability:"
echo "     - Select a session"
echo "     - Move it up/down"
echo "     - Selection should follow the moved session"
echo ""
echo "  D. Test prefix key display:"
echo "     - Press Ctrl+B and hold"
echo "     - Footer should show: ⌨ ^B → with move commands"
echo "     - Footer shows: ↑/↓: move session"
echo ""
echo "  E. Test prefix centralization:"
echo "     - Edit src/client.rs"
echo "     - Change: const PREFIX_KEY: char = 'b';"
echo "     - To:     const PREFIX_KEY: char = 'a';"
echo "     - Rebuild: cargo build --release"
echo "     - Start client again"
echo "     - Verify Ctrl+A works as prefix instead of Ctrl+B"
echo "     - Footer should show ^A everywhere"
echo ""
echo "  F. Verify existing functionality still works:"
echo "     - Ctrl+B d → detach (should work)"
echo "     - Ctrl+B q → shutdown (should work)"
echo "     - Ctrl+B Tab → switch to terminal (should work)"
echo "     - Ctrl+B Ctrl+B → send literal Ctrl+B to terminal"
echo "     - j/k navigation (should work)"
echo "     - n for new session (should work)"
echo "     - r for rename (should work)"
echo "     - d for delete (should work)"
echo ""
echo "Cleanup:"
echo "  - Ctrl+B q to shutdown"
echo "  - Or: pkill -f 'mbulet.*daemon'"
echo "  - rm /tmp/mbulet-test.sock"
echo ""
